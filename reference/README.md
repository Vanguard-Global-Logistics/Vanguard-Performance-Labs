# reference/ — approved visual source of truth

Commit the owner-approved mockups here with EXACTLY these names so VISUAL-QA can compare against them:
- approved-desktop.png
- approved-mobile.png
- approved-hero.png

STATUS: none committed yet (recorded in docs/ASSET-MANIFEST.md). The two mockups shared in chat
(dashboard-forward + hero-forward) should be exported to these filenames. Never overwrite an approved
file once present.
